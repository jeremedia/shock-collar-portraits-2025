<% content_for :title, "New Tag" %>

<div class="bg-gray-900 text-white p-6 min-h-screen">
  <div class="max-w-3xl mx-auto">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-yellow-500 mb-2">Create New Tag</h1>
      <p class="text-gray-400">Add a new tag to the system for categorizing photos.</p>
    </div>

    <!-- Form -->
    <div class="bg-gray-800 rounded-lg p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>