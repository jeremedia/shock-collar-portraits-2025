<!DOCTYPE html>
<html>
  <head>
    <title>Face Detection Queue - Admin</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="turbo-visit-control" content="reload">
  </head>

  <body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-2">Face Detection Queue</h1>
        <p class="text-gray-600">Monitor and control face detection processing</p>
      </div>

      <!-- Flash Messages -->
      <% if flash[:success] %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= flash[:success] %>
        </div>
      <% end %>
      
      <% if flash[:error] %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= flash[:error] %>
        </div>
      <% end %>

      <% if flash[:info] %>
        <div class="bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded mb-4">
          <%= flash[:info] %>
        </div>
      <% end %>

      <!-- Navigation -->
      <div class="mb-8 flex space-x-4">
        <a href="/admin" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
          ← Admin Dashboard
        </a>
        <a href="/" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
          ← Back to Gallery
        </a>
      </div>

      <!-- Face Detection Stats -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 mb-8">
        <div class="bg-white rounded-lg shadow p-6">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M4 3a2 2 0 100 4h12a2 2 0 100-4H4z"/>
                  <path fill-rule="evenodd" d="M3 8h14v7a2 2 0 01-2 2H5a2 2 0 01-2-2V8zm5 3a1 1 0 011-1h2a1 1 0 110 2H9a1 1 0 01-1-1z" clip-rule="evenodd"/>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Total Photos</dt>
                <dd class="text-lg font-medium text-gray-900"><%= number_with_delimiter(@stats[:total_photos]) %></dd>
              </dl>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Processed</dt>
                <dd class="text-lg font-medium text-gray-900">
                  <%= number_with_delimiter(@stats[:processed_photos]) %>
                  <span class="text-sm text-gray-500">(<%= @stats[:processing_percentage] %>%)</span>
                </dd>
              </dl>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-yellow-500 rounded-full flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Pending</dt>
                <dd class="text-lg font-medium text-gray-900"><%= number_with_delimiter(@stats[:unprocessed_photos]) %></dd>
              </dl>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-purple-500 rounded-full flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-6-3a2 2 0 11-4 0 2 2 0 014 0zm-2 4a5 5 0 00-4.546 2.916A5.986 5.986 0 0010 16a5.986 5.986 0 004.546-2.084A5 5 0 0010 11z" clip-rule="evenodd"/>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">With Faces</dt>
                <dd class="text-lg font-medium text-gray-900"><%= number_with_delimiter(@stats[:photos_with_faces]) %></dd>
              </dl>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-gray-500 rounded-full flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M13.477 14.89A6 6 0 015.11 6.524l8.367 8.368zm1.414-1.414L6.524 5.11a6 6 0 018.367 8.367zM18 10a8 8 0 11-16 0 8 8 0 0116 0z" clip-rule="evenodd"/>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">No Faces</dt>
                <dd class="text-lg font-medium text-gray-900"><%= number_with_delimiter(@stats[:photos_without_faces]) %></dd>
              </dl>
            </div>
          </div>
        </div>
      </div>

      <!-- Queue Statistics -->
      <div class="bg-white rounded-lg shadow mb-8">
        <div class="px-6 py-4 border-b border-gray-200">
          <h3 class="text-lg font-medium text-gray-900">Solid Queue Statistics</h3>
        </div>
        <div class="p-6">
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div class="text-center">
              <div class="text-2xl font-bold text-blue-600"><%= number_with_delimiter(@queue_stats[:pending_jobs] || 0) %></div>
              <div class="text-sm text-gray-600">Pending Jobs</div>
            </div>
            <div class="text-center">
              <div class="text-2xl font-bold text-green-600"><%= number_with_delimiter(@queue_stats[:finished_jobs] || 0) %></div>
              <div class="text-sm text-gray-600">Completed Jobs</div>
            </div>
            <div class="text-center">
              <div class="text-2xl font-bold text-red-600"><%= number_with_delimiter(@queue_stats[:failed_jobs] || 0) %></div>
              <div class="text-sm text-gray-600">Failed Jobs</div>
            </div>
          </div>
          
          <div class="mt-4 pt-4 border-t border-gray-200">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div class="text-center">
                <div class="text-xl font-bold text-purple-600"><%= number_with_delimiter(@queue_stats[:pending_face_jobs] || 0) %></div>
                <div class="text-sm text-gray-600">Pending Face Detection Jobs</div>
              </div>
              <div class="text-center">
                <div class="text-xl font-bold text-orange-600"><%= number_with_delimiter(@queue_stats[:failed_face_jobs] || 0) %></div>
                <div class="text-sm text-gray-600">Failed Face Detection Jobs</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Queue Controls -->
      <div class="bg-white rounded-lg shadow mb-8">
        <div class="px-6 py-4 border-b border-gray-200">
          <h3 class="text-lg font-medium text-gray-900">Queue Controls</h3>
        </div>
        <div class="p-6">
          <div class="flex flex-wrap gap-4">
            <%= form_with url: admin_enqueue_all_path, method: :post, local: true, class: "inline" do |form| %>
              <%= form.submit "Process All Unprocessed Photos (#{@stats[:unprocessed_photos]})", 
                  class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded",
                  confirm: "This will enqueue #{@stats[:unprocessed_photos]} photos for face detection. Continue?" %>
            <% end %>

            <% if @queue_stats[:failed_face_jobs] && @queue_stats[:failed_face_jobs] > 0 %>
              <%= form_with url: admin_retry_failed_path, method: :post, local: true, class: "inline" do |form| %>
                <%= form.submit "Retry Failed Jobs (#{@queue_stats[:failed_face_jobs]})", 
                    class: "bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded" %>
              <% end %>
            <% end %>

            <% if @queue_stats[:finished_jobs] && @queue_stats[:finished_jobs] > 0 %>
              <%= form_with url: admin_clear_completed_jobs_path, method: :post, local: true, class: "inline" do |form| %>
                <%= form.submit "Clear Completed Jobs (#{@queue_stats[:finished_jobs]})", 
                    class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded",
                    confirm: "This will permanently remove #{@queue_stats[:finished_jobs]} completed jobs from the queue. Continue?" %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Recent Face Detection Jobs -->
      <% if @recent_face_jobs.any? %>
        <div class="bg-white rounded-lg shadow mb-8">
          <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-gray-900">Recent Face Detection Jobs</h3>
          </div>
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Photo ID</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Created</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Finished</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Duration</th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <% @recent_face_jobs.each do |job| %>
                  <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                      <% if job.arguments.present? %>
                        Photo #<%= job.arguments.first %>
                      <% else %>
                        —
                      <% end %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      <% if job.finished_at %>
                        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                          Completed
                        </span>
                      <% else %>
                        <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                          Pending
                        </span>
                      <% end %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                      <%= job.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                      <%= job.finished_at&.strftime("%Y-%m-%d %H:%M:%S") || "—" %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                      <% if job.finished_at && job.created_at %>
                        <%= ((job.finished_at - job.created_at) / 1.minute).round(2) %>min
                      <% else %>
                        —
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      <% end %>

      <!-- Failed Face Detection Jobs -->
      <% if @failed_face_jobs.any? %>
        <div class="bg-white rounded-lg shadow mb-8">
          <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-red-600">Failed Face Detection Jobs</h3>
          </div>
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Photo ID</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Error</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Failed At</th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <% @failed_face_jobs.each do |failed_job| %>
                  <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                      <% if failed_job.job.arguments.present? %>
                        Photo #<%= failed_job.job.arguments.first %>
                      <% else %>
                        —
                      <% end %>
                    </td>
                    <td class="px-6 py-4 text-sm text-gray-500 max-w-xs">
                      <div class="truncate" title="<%= failed_job.error %>">
                        <%= failed_job.error.to_s.first(80) %><%= "..." if failed_job.error.to_s.length > 80 %>
                      </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                      <%= failed_job.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      <% end %>

      <!-- Progress Information -->
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h4 class="text-lg font-medium text-blue-900 mb-4">Face Detection Progress</h4>
        <div class="w-full bg-gray-200 rounded-full h-3 mb-4">
          <div class="bg-blue-600 h-3 rounded-full" style="width: <%= @stats[:processing_percentage] %>%"></div>
        </div>
        <div class="flex justify-between text-sm text-blue-800">
          <span><%= number_with_delimiter(@stats[:processed_photos]) %> processed</span>
          <span><%= @stats[:processing_percentage] %>% complete</span>
          <span><%= number_with_delimiter(@stats[:unprocessed_photos]) %> remaining</span>
        </div>
      </div>

      <!-- Auto-refresh script -->
      <script>
        // Auto-refresh every 15 seconds for more real-time monitoring
        setTimeout(function() {
          window.location.reload();
        }, 15000);
      </script>
    </div>
  </body>
</html>